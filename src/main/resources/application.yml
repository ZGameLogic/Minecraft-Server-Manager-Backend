spring:
  application:
    name: minecraft-server-manager
  config:
    import: configserver:http://192.168.1.15:30050
  cloud:
    config:
      fail-fast: true
      retry:
        max-attempts: 200